{
  "$schema": "https://json.schemastore.org/mcp.json",
  "_comment": "cc-relay probe — verifies whether Claude Code on Web honors repo-local .mcp.json stdio entries. After this is committed and the session refreshed, /tmp/cc-relay-mcp-probe.log will be written if it works. The `probe-stdio` server is a shell script that logs invocation and speaks minimal MCP (initialize + tools/list + probe_ping tool). Delete this file once we have a verdict.",
  "mcpServers": {
    "probe-stdio": {
      "type": "stdio",
      "command": "bash",
      "args": ["hooks/probe-mcp.sh"]
    }
  }
}
