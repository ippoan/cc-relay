// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { FileEventKind } from "./FileEventKind";
import type { Hello } from "./Hello";
import type { NotifyTarget } from "./NotifyTarget";
import type { PlanOp } from "./PlanOp";
import type { Priority } from "./Priority";
import type { TaskSpec } from "./TaskSpec";

/**
 * Top-level message exchanged over the WebSocket.
 *
 * `#[serde(tag = "type", rename_all = "snake_case")]` produces a
 * discriminated union that survives the Rust ↔ TypeScript boundary cleanly.
 */
export type WireMessage = { "type": "hello" } & Hello | { "type": "hello_ack", 
/**
 * Echoed back from `Hello.agent_id` for sanity-checking.
 */
agent_id: string, 
/**
 * Server-side wall clock in millis since epoch.
 */
server_time: number, 
/**
 * Same as [`PROTOCOL_VERSION`] on the coordinator side.
 */
protocol_version: number, } | { "type": "agent_joined", agent_id: string, } | { "type": "agent_left", agent_id: string, } | { "type": "file_event", agent_id: string, path: string, kind: FileEventKind, timestamp: number, } | { "type": "notify_agent", from: string, to: NotifyTarget, message: string, priority: Priority, timestamp: number, } | { "type": "plan_op", op: PlanOp, } | { "type": "plan_snapshot", tasks: Array<TaskSpec>, } | { "type": "error", code: number, message: string, };
