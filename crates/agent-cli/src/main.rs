//! `rust-mcp-agent` — clap dispatcher into `agent-daemon` and `agent-mcp`.
//!
//! For P0 (#2 + #3) this is intentionally a stub so the workspace builds
//! end-to-end on a fresh clone. Subcommands land in P2 (#5, #6).

fn main() {
    eprintln!("rust-mcp-agent: not yet implemented (P0 scaffold).");
    eprintln!("Track progress at https://github.com/ippoan/cc-relay/issues/1");
    std::process::exit(1);
}
